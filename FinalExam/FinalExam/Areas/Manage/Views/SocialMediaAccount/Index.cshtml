@model List<SocialMediaAccount>
@{
    int count = 0;
}
<div class="container">
    <div class="d-flex justify-content-between align-content-center">
        <div>Create a new Team member</div>
        <div>
            <a class="btn btn-success" asp-controller="socialmediaaccount" asp-action="create">+Create</a>
        </div>
    </div>
    <div>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Icon</th>
                    <th scope="col">Url</th>
                    <th scope="col">Update</th>
                    <th scope="col">Delete</th>
                </tr>
            </thead>
            <tbody>

                @foreach (var socialMediaAccount in Model)
                {
                    count++;
                    <tr>
                        <th scope="row">@count</th>
                        <th scope="row">@socialMediaAccount.TeamMember?.FullName</th>
                        <td >
                          @socialMediaAccount.Icon
                        </td>
                        <td><a href="@socialMediaAccount.Url">Url</a></td>
                        <td>
                            <a class="btn btn-primary" asp-controller="socialmediaaccount" asp-action="update" asp-route-id="@socialMediaAccount.Id">Update</a>
                        </td>
                        <td>
                            <a class="btn btn-danger" asp-controller="socialmediaaccount" asp-action="delete" asp-route-id="@socialMediaAccount.Id">Update</a>
                        </td>
                    </tr>
                }
              

              
            </tbody>
        </table>
        @*<form asp-action="index" method="get">

            <nav aria-label="Page navigation example">
                @if (Model.HasPrevious)
                {
                    <li class="page-item"><a class="page-link" asp-area="manage" asp-controller="SocialMediaAccount" asp-action="index" asp-route-page="@(Model.Page-1)">Previous</a></li>
                }
                <ul class="pagination">
                    @for (int i = 1; i < Model.TotalPage; i++)
                    {
                        <li class="page-item @(Model.Page==i ? "active" : "")">
                            <a asp-area="manage" asp-controller="SocialMediaAccount" asp-action="index" asp-route-page="@i">@i</a>
                        </li>
                    }
                </ul>
                @if (Model.HasNext)
                {
                    <li class="page-item"><a class="page-link" asp-area="manage" asp-controller="SocialMediaAccount" asp-action="index" asp-route-page="@(Model.Page+1)">Next</a></li>
                }
            </nav>
        </form>*@
    </div>
</div>